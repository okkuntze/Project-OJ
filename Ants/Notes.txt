#include <bits/stdc++.h> 
using namespace std; 

class sys
{	
	public:
	int NumberOfPlayer = 2; 
	void start()
	{
		string x;
		cout << "Hello" << endl;
		cout << " Do you want to start? [Y/N]" << endl;
		cin >> x;
		if (x == Y || x == y):
			{	sys.startgame(); 
			}
		else { sys.start();
		}

	void startgame(){
		// setup first game state
		// option default or set
		Map.createMap();
	}
	void GetNumberOfPlayer(){
		cout << "Choose the amount of Players" << endl;
		cin >> NumberOfPlayer >> endl;
		sys.SetNumberOfPlayer(NumberOfPlayer);
	}
	void SetNumberOfPlayer(int = 2)
};


class Ants
{ 
    private:
    public: 
}; 

class Ameisenbau
{
};

class Resource
{
};

class Map
{
	private:
	Dimesnion;
	void create_map(){
		auto Dimension = Map.SetDimensions();
		auto Player = 
	}

	auto SetDimensions(int = 20){	
		auto D;	
		cout << "Set the size of the world" << endl;
		cout << "Dimension:  " <<;
		cin >> D >> endl; // check if number
		return D;
	};

class units
{
};

int main() {
sys.start();
}